.env
*.env
!.env.example

steward/data/
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm

__pycache__/
*.pyc
.venv/
venv/

# The working copy of the font, converted from the game's bundled woff2.
# Regenerate it rather than committing a binary nobody diffs.
brand/cinzel.ttf

# Downloaded OFL faces. brand/getfonts.py fetches them.
brand/fonts/

# Copies of files an update was about to overwrite.
backups/

# Built distributions.
dist/

# Your own calendar edits, written by the setup page. Per deployment,
# like .env, and never replaced by an update.
blueprint/calendars/*.local.yaml
blueprint/variables.local.yaml
blueprint/content-calendar.local.yaml
